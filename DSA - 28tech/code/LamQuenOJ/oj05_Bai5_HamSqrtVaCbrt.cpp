#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n;

    cout << fixed << setprecision(2) << sqrt(n) << endl;
    cout << fixed << setprecision(3) << cbrt(n) << endl;

    return 0;
}
